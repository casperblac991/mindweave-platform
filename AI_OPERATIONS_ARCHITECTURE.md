# بنية تشغيل الذكاء الاصطناعي في MindWeave

## الهدف التشغيلي

تتكون الطبقة الذكية من مساعد محادثة داخل المنصة، وواجهة إدارية لمراجعة المواد الجديدة، ودورة محتوى تجمع مصادر موثوقة وتحولها إلى مسودات فقط. لا ينشر النظام نصاً جديداً مباشرة إلى الواجهة العامة من دون أن تتغير حالة المسودة إلى `approved` أو `published` بواسطة مستخدم إداري.

## طبقات الاعتمادية

| الطبقة | المسؤولية | سلوك التعطل |
|---|---|---|
| الواجهة | واجهة محادثة بسيطة ومتاحة في الصفحات الأساسية | تعرض رسالة واضحة وتحفظ الصفحة قابلة للاستخدام إذا كانت خدمة الذكاء الاصطناعي غير مهيأة أو غير متاحة. |
| خادم API | التحقق من المدخلات، تحديد المعدل، إزالة الأسرار من العميل، واستدعاء النموذج | يعيد أخطاء عامة آمنة ولا يمرر معلومات البنية الداخلية أو مفتاح الوصول. |
| قاعدة البيانات | المسودات، المصادر، الدور الإداري، وسجل العمليات | سياسات وصول صفية تمنع الزوار من الوصول إلى المسودات أو سجل المحادثة. |
| دورة المحتوى | جمع RSS/المصادر المصرح بها، إزالة التكرار، التصنيف، وصناعة مسودة | العمل متكرر وآمن عند الإعادة؛ لا ينفذ شيئاً عندما تنقص الأسرار أو المصادر. |
| المراقبة | نقطة صحة وخدمة حالة آمنة وسجل للمهام | تفصل توافر الموقع عن توافر الذكاء الاصطناعي حتى لا يتوقف الموقع كاملاً. |

## سياسة الإجابة

يستخدم المساعد نموذج `gpt-5-mini` في المحادثات الاعتيادية وصناعة المسودات. لا يدعي امتلاك معلومات غير موجودة في مواد MindWeave، ويطلب التوضيح عند الحاجة، ويشير إلى أن المحتوى التعليمي لا يحل محل النصيحة الطبية أو القانونية أو المالية المتخصصة. يرفض الطلبات الضارة أو التي تتطلب أسراراً أو بيانات شخصية حساسة.

## سياسة المحتوى

تقتصر دورة المواد على عناوين وموجزات المصادر المحددة صراحة. تستخرج المرشحات، وتزيل التكرار، وتنتج مسودة منظمة بعلامة مصدر ولغة وتصنيف وتقييم صلة. لا تكفي حالة `draft` للظهور العام؛ النشر يتطلب مراجعاً إدارياً.

## متطلبات التفعيل في الاستضافة

يُحفظ مفتاح `OPENAI_API_KEY` داخل متغيرات أسرار الخادم فقط، ولا يظهر في المتصفح أو المستودع. تعتمد لوحة العمليات على جلسة Supabase للمستخدم المصرح له وسياسات RLS مقيدة، لذلك لا تحتاج إلى مفتاح خدمة واسع الصلاحيات. تظل المنصة في وضع آمن إذا غاب مفتاح النموذج. لتشغيل دورة المحتوى دورياً، يلزم عامل مجدول موثوق يملك تفويضاً محدوداً للكتابة في المسودات؛ لا تُفعّل الدورة حتى يتوفر هذا العامل.

## حدود الاستمرارية

يوفر التصميم نقاط صحة، استرداداً آمناً، وحدود معدل، وفصلاً بين الموقع والمساعد. لا يمكن لأي تطبيق أن يضمن انعدام الانقطاع بشكل مطلق لأن مزود الاستضافة والشبكة ومزود النموذج أطراف خارجية؛ لذلك يعالج التصميم الأعطال المتوقعة بحيث يظل المتجر والواجهة الأساسية متاحين حتى عند فشل خدمة الذكاء الاصطناعي.

## ملاحظة توافق مزود النموذج

أشارت وثائق Groq إلى أن واجهته متوافقة إلى حد كبير مع صيغة OpenAI، ما يجعلها خياراً قابلاً للاختبار من خلال نقطة الخدمة المتوافقة قبل إضافة أي مفتاح جديد إلى Render.[1]

## المراجع

[1] [GroqDocs — OpenAI Compatibility](https://console.groq.com/docs/openai)
