# دليل التسجيل وجمع الإيميلات في MindWeave

## ما الذي تم إصلاحه؟

تم تحديث مسار إنشاء الحساب بحيث يتم إنشاء المستخدم في Supabase Auth أولاً، ثم حفظ ملفه الشخصي وجمع بريده للنشرة البريدية بشكل منفصل. لذلك لن يفشل إنشاء الحساب إذا كانت سياسة جدول `user_profiles` غير مضبوطة؛ سيظهر تحذير في السجل فقط، بينما يظل الحساب صالحاً للتفعيل وتسجيل الدخول.

كما تم تحديث نموذج الاشتراك الموجود في الصفحة الرئيسية. أصبح البريد يرسل إلى جدول `newsletter_subscribers` عبر `upsert` لمنع التكرار، ويحفظ النظام نسخة محلية مؤقتة عند انقطاع الاتصال حتى لا يفقد البريد المدخل. لا تُعرض قائمة الإيميلات للزوار، لأن كشفها في الواجهة الأمامية يمثل خطراً على الخصوصية.

## طريقة تسجيل مستخدم جديد

افتح صفحة [إنشاء الحساب](https://mindweave.store/signup.html)، ثم أدخل الاسم الكامل، والبريد الإلكتروني، وكلمة مرور لا تقل عن ستة أحرف. بعد الضغط على **إنشاء الحساب** ستظهر رسالة واضحة بحالة الطلب. إذا كان تأكيد البريد مفعلاً في Supabase، افتح رسالة التفعيل التي تصل إلى البريد، ثم اضغط الرابط، وبعد ذلك انتقل إلى صفحة [تسجيل الدخول](https://mindweave.store/login.html) وأدخل البريد وكلمة المرور.

إذا ظهر أن البريد مسجل مسبقاً، استخدم صفحة تسجيل الدخول بدلاً من إنشاء حساب جديد. وإذا لم تصل رسالة التفعيل، افحص مجلد الرسائل غير المرغوب فيها، وتأكد من إضافة `mindweave.store` إلى النطاقات المسموح بها في إعدادات البريد، ثم راجع إعدادات Supabase الموضحة أدناه.

## طريقة جمع إيميلات العملاء

يمكن للزائر إدخال بريده في نموذج **احصل على 10 أوامر مجانية** الموجود أسفل الصفحة الرئيسية. عند الضغط على زر الاشتراك، يتحقق النظام من صيغة البريد، ثم يرسله إلى جدول `newsletter_subscribers` مع تاريخ الاشتراك ومصدر التسجيل. عند إرسال البريد من صفحة إنشاء الحساب، يسجل النظام المصدر `signup`، وعند إرساله من الصفحة الرئيسية يسجل المصدر `homepage`.

يجب أن تتضمن رسالة الحملة التسويقية سبب جمع البريد وطريقة إلغاء الاشتراك. لا تستخدم القائمة إلا لإرسال محتوى يتوقعه المشترك، والتزم بالقوانين المحلية وقواعد مكافحة الرسائل المزعجة. وجود موافقة واضحة وإمكانية إلغاء الاشتراك يحمي سمعة نطاقك ويزيد معدل وصول الرسائل.

## إعداد Supabase لأول مرة

افتح مشروع Supabase المرتبط بالمنصة، ثم اذهب إلى **SQL Editor** والصق محتوى ملف `SUPABASE_SETUP.sql` الموجود في هذا المستودع، ثم نفّذه مرة واحدة. ينشئ الملف جدولَي `user_profiles` و`newsletter_subscribers`، ويضيف سياسات RLS تسمح بإنشاء الاشتراك فقط دون كشف قائمة الإيميلات للعامة.

بعد ذلك افتح **Authentication > URL Configuration** واضبط **Site URL** على:

```text
https://mindweave.store
```

وأضف رابط إعادة التوجيه التالي:

```text
https://mindweave.store/login.html
```

تأكد أيضاً من تفعيل مزود البريد الإلكتروني. لا تضع مفتاح `service_role` في ملفات HTML؛ المفتاح الموجود في الواجهة يجب أن يكون مفتاح `anon` فقط مع تفعيل RLS.

## استخراج قائمة الإيميلات

لأسباب أمنية، لا يتم إنشاء صفحة عامة تعرض المشتركين. لاستخراج القائمة، استخدم SQL Editor داخل Supabase وشغّل الاستعلام التالي:

```sql
select email, subscribed_at, source
from public.newsletter_subscribers
where is_active = true
order by subscribed_at desc;
```

بعد ظهور النتائج، استخدم خيار التصدير إلى CSV من لوحة Supabase، ثم افتح الملف في Excel أو ارفعه إلى Google Sheets. يجب حفظ الملف في مساحة عمل خاصة وعدم مشاركته مع أشخاص غير مخولين.

## التحقق من عمل النظام

اختبر التسجيل أولاً ببريد اختبار حقيقي، لأن Supabase قد يمنع العناوين الوهمية. جرّب بعد ذلك إدخال البريد نفسه في نموذج النشرة للتأكد من عدم إنشاء صف مكرر. ثم جرّب بريداً جديداً من نافذة خاصة أو جهاز آخر، وتحقق من ظهور صف جديد في جدول `newsletter_subscribers` داخل Supabase.

إذا ظهر في الموقع تنبيه يفيد بحفظ البريد مؤقتاً، فهذا يعني أن المتصفح لم يستطع الوصول إلى جدول Supabase. في هذه الحالة نفّذ ملف `SUPABASE_SETUP.sql`، وتحقق من رابط المشروع ومفتاح `anon` وإعدادات RLS، ثم أعد المحاولة. البريد المؤقت محفوظ على الجهاز فقط ولا يُعد بديلاً عن مزامنة قاعدة البيانات.

## الملفات التي تم تحديثها

| الملف | الوظيفة |
|---|---|
| `supabase-auth.js` | إصلاح إنشاء الحساب، الرسائل، جمع البريد، وإعادة المحاولة المحلية. |
| `signup.html` | واجهة تسجيل أوضح، تحقق من كلمة المرور، ورسائل حالة داخل الصفحة. |
| `index.html` | ربط نموذج النشرة بقاعدة البيانات بدلاً من `localStorage` فقط. |
| `SUPABASE_SETUP.sql` | إنشاء الجداول والسياسات والتريغر المطلوب. |
| `EMAILS_AND_AUTH_GUIDE.md` | خطوات الإعداد والتسجيل والاستخراج والاختبار. |

> **ملاحظة مهمة:** لا يمكن للنسخة الأمامية وحدها إرسال حملات بريدية جماعية بأمان. بعد استخراج القائمة، استخدم مزوداً متخصصاً مثل Mailchimp أو Brevo أو خدمة بريدية مماثلة، مع التأكد من وجود رابط إلغاء اشتراك في كل رسالة.
